<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:StreamScanner.Maui.ViewModels"
             xmlns:models="clr-namespace:StreamScanner.Maui.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="StreamScanner.Maui.Views.MainPage"
             x:DataType="viewmodels:MainViewModel"
             BackgroundColor="{StaticResource BgPrimary}">

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <!-- Header -->
        <Grid Grid.Row="0"
              BackgroundColor="{StaticResource BgSecondary}"
              Padding="20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <HorizontalStackLayout Grid.Column="0" Spacing="12">
                <Border BackgroundColor="{StaticResource AccentRed}"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 10"
                        WidthRequest="44"
                        HeightRequest="44">
                    <Label Text="▶"
                           TextColor="White"
                           FontSize="20"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </Border>
                <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                    <Label Text="Stream Scanner"
                           Style="{StaticResource HeaderTitle}" />
                    <Label Text="YOUTUBE LIVE DISCOVERY"
                           Style="{StaticResource HeaderSubtitle}" />
                </VerticalStackLayout>
            </HorizontalStackLayout>

            <!-- Status Badge and Settings -->
            <HorizontalStackLayout Grid.Column="1" Spacing="12" VerticalOptions="Center">
                <Frame Style="{StaticResource StatusBadge}">
                    <Label Text="{Binding StatusMessage}"
                           TextColor="{StaticResource TextSecondary}"
                           FontSize="13" />
                </Frame>

                <!-- Settings Button -->
                <Border BackgroundColor="{StaticResource BgTertiary}"
                        Stroke="{StaticResource BorderPrimary}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 10"
                        WidthRequest="44"
                        HeightRequest="44">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnSettingsTapped" />
                    </Border.GestureRecognizers>
                    <Label Text="⚙️"
                           FontSize="18"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </Border>
            </HorizontalStackLayout>
        </Grid>

        <!-- Controls Section -->
        <Grid Grid.Row="1"
              BackgroundColor="{StaticResource BgSecondary}"
              Padding="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search Input -->
            <Border Grid.Row="0"
                    Style="{StaticResource InputBorder}"
                    Margin="0,0,0,12">
                <Entry Text="{Binding Keywords}"
                       Placeholder="Enter keywords (comma-separated)"
                       Style="{StaticResource SearchEntry}"
                       ReturnCommand="{Binding ScanCommand}"
                       VerticalOptions="Center" />
            </Border>

            <!-- Options Row -->
            <Grid Grid.Row="1" ColumnDefinitions="*,*,Auto" ColumnSpacing="12">

                <!-- Max Results Picker -->
                <VerticalStackLayout Grid.Column="0" Spacing="6">
                    <Label Text="RESULTS" Style="{StaticResource LabelText}" />
                    <Border Style="{StaticResource InputBorder}">
                        <Picker ItemsSource="{Binding MaxResultsOptions}"
                                SelectedItem="{Binding SelectedMaxResults}"
                                Style="{StaticResource OptionPicker}"
                                VerticalOptions="Center" />
                    </Border>
                </VerticalStackLayout>

                <!-- Refresh Interval Picker -->
                <VerticalStackLayout Grid.Column="1" Spacing="6">
                    <Label Text="AUTO-REFRESH" Style="{StaticResource LabelText}" />
                    <Border Style="{StaticResource InputBorder}">
                        <Picker ItemsSource="{Binding RefreshOptions}"
                                ItemDisplayBinding="{Binding Display}"
                                SelectedItem="{Binding SelectedRefreshInterval, Converter={StaticResource RefreshOptionConverter}}"
                                Style="{StaticResource OptionPicker}"
                                VerticalOptions="Center">
                            <Picker.Behaviors>
                                <toolkit:EventToCommandBehavior
                                    EventName="SelectedIndexChanged"
                                    Command="{Binding StopAutoRefreshCommand}" />
                            </Picker.Behaviors>
                        </Picker>
                    </Border>
                </VerticalStackLayout>

                <!-- Scan Button -->
                <Button Grid.Column="2"
                        Text="Scan Now"
                        Command="{Binding ScanCommand}"
                        IsEnabled="{Binding IsScanning, Converter={toolkit:InvertedBoolConverter}}"
                        Style="{StaticResource PrimaryButton}"
                        VerticalOptions="End" />
            </Grid>
        </Grid>

        <!-- Results Area -->
        <Grid Grid.Row="2" Padding="20,16">

            <!-- Empty State -->
            <VerticalStackLayout IsVisible="{Binding ShowEmptyState}"
                                 VerticalOptions="Center"
                                 HorizontalOptions="Center"
                                 Spacing="12">
                <Label Text="📺"
                       FontSize="60"
                       HorizontalOptions="Center"
                       Opacity="0.5" />
                <Label Text="Ready to Scan"
                       Style="{StaticResource EmptyStateTitle}" />
                <Label Text="Enter keywords and click &quot;Scan Now&quot; to discover live streams"
                       Style="{StaticResource EmptyStateText}" />
            </VerticalStackLayout>

            <!-- No Results State -->
            <VerticalStackLayout IsVisible="{Binding ShowNoResults}"
                                 VerticalOptions="Center"
                                 HorizontalOptions="Center"
                                 Spacing="12">
                <Label Text="🔍"
                       FontSize="60"
                       HorizontalOptions="Center"
                       Opacity="0.5" />
                <Label Text="No Live Streams Found"
                       Style="{StaticResource EmptyStateTitle}" />
                <Label Text="Try different keywords or check back later"
                       Style="{StaticResource EmptyStateText}" />
            </VerticalStackLayout>

            <!-- Loading Indicator -->
            <ActivityIndicator IsRunning="{Binding IsScanning}"
                               IsVisible="{Binding IsScanning}"
                               HeightRequest="50"
                               WidthRequest="50"
                               VerticalOptions="Center"
                               HorizontalOptions="Center" />

            <!-- Results List -->
            <CollectionView ItemsSource="{Binding Streams}"
                            IsVisible="{Binding HasResults}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:LiveStream">
                        <Frame Style="{StaticResource CardFrame}">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=OpenStreamCommand}"
                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>

                            <Grid ColumnDefinitions="160,*,Auto" ColumnSpacing="16">

                                <!-- Thumbnail -->
                                <Grid Grid.Column="0">
                                    <Border StrokeThickness="0"
                                            StrokeShape="RoundRectangle 10"
                                            HeightRequest="90">
                                        <Image Source="{Binding ThumbnailUrl}"
                                               Aspect="AspectFill" />
                                    </Border>

                                    <!-- Live Badge -->
                                    <Frame Style="{StaticResource LiveBadge}"
                                           HorizontalOptions="Start"
                                           VerticalOptions="Start"
                                           Margin="8">
                                        <HorizontalStackLayout Spacing="6">
                                            <Ellipse Fill="White"
                                                     WidthRequest="6"
                                                     HeightRequest="6" />
                                            <Label Text="LIVE"
                                                   TextColor="White"
                                                   FontSize="10"
                                                   FontAttributes="Bold"
                                                   CharacterSpacing="0.5" />
                                        </HorizontalStackLayout>
                                    </Frame>
                                </Grid>

                                <!-- Stream Info -->
                                <VerticalStackLayout Grid.Column="1"
                                                     VerticalOptions="Center"
                                                     Spacing="6">
                                    <Label Text="{Binding Title}"
                                           Style="{StaticResource StreamTitle}" />
                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="👤"
                                               FontSize="12" />
                                        <Label Text="{Binding ChannelTitle}"
                                               Style="{StaticResource StreamMeta}" />
                                    </HorizontalStackLayout>
                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="🕐"
                                               FontSize="12" />
                                        <Label Text="{Binding StartTimeDisplay, StringFormat='Started {0}'}"
                                               Style="{StaticResource StreamMeta}" />
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>

                                <!-- Viewer Count -->
                                <Frame Grid.Column="2"
                                       Style="{StaticResource ViewerBadge}"
                                       VerticalOptions="Center">
                                    <HorizontalStackLayout Spacing="8">
                                        <Label Text="👥"
                                               FontSize="14"
                                               TextColor="{StaticResource AccentRed}" />
                                        <VerticalStackLayout>
                                            <Label Text="{Binding ViewerCountDisplay}"
                                                   Style="{StaticResource ViewerCount}" />
                                            <Label Text="viewers"
                                                   Style="{StaticResource ViewerLabel}" />
                                        </VerticalStackLayout>
                                    </HorizontalStackLayout>
                                </Frame>

                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="3"
              BackgroundColor="{StaticResource BgSecondary}"
              Padding="20,16">
            <Label Text="Powered by YouTube Data API v3"
                   TextColor="{StaticResource TextMuted}"
                   FontSize="12"
                   HorizontalOptions="Center" />
        </Grid>

    </Grid>
</ContentPage>
